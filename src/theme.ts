
import { createTheme } from '@mui/material/styles';
 
export const darkTheme = createTheme({
    palette: {
        mode: 'light',
    },
    typography: {
        h6: {
            fontSize: "1rem"
        }
    }
}); 